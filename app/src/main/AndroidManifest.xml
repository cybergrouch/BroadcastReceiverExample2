<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.android.training.d1.brdcastRcvrEx2"
      android:versionCode="1"
      android:versionName="1.0">
    <application android:icon="@drawable/icon" android:label="@string/app_name">
    
        <activity android:name=".BroadcastReceiverExample2"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name="SampleActivity1" />
        
        <activity android:name="SampleActivity2" />
        
        <!-- Can also be registered via code. 
        <receiver android:name="MyReceiver">
        	<intent-filter>
        		<action android:name="1" />
        		<action android:name="2" />
        	</intent-filter>
        </receiver>-->

    </application>
    <uses-sdk android:minSdkVersion="6" />

</manifest> 